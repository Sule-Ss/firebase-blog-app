import React from "react";
import BlogCard from "../components/BlogCard";

const Dashboard = () => {
  return (
    <div>
      {/*  {datas.map((data) =>(
        <BlogCard key={} {...data}/>
      ))} */}
      <p>DashBoard</p>
      <BlogCard />
    </div>
  );
};

export default Dashboard;
